{
  "framework": null,
  "installCommand": "pnpm install",
  "buildCommand": "NODE_OPTIONS=\"--max-old-space-size=3072\" pnpm --filter @workspace/numatik run build",
  "outputDirectory": "artifacts/numatik/dist/public",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
